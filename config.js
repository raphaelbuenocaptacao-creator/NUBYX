// Public runtime configuration for NUBYX.
// Supabase URL and anon key are designed to be public client configuration.
// Never place service_role keys or other secrets in this file.
window.NUBYX_CONFIG = Object.freeze({
  supabaseUrl: '',
  supabaseAnonKey: '',
  authEnabled: false
});
